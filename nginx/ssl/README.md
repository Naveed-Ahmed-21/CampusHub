# CampusHub SSL Certificate Directory
# For production deployment, generate Let's Encrypt certificates using Certbot:
# certbot certonly --webroot -w /var/www/certbot -d campushub.edu -d www.campushub.edu
